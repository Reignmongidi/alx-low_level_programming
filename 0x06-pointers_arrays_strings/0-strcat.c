#include "main.h"
#include <string.h>

/**
 *_strcat - Concatenates two strings
 *
 * Main - Check the code
 * Return: Always zero
 *
 **/

char *_strcat(char *dest, char *src)
{

	strcat(dest, src);
return (0);
}
